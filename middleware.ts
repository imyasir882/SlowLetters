// Disable middleware for now - handle auth in API routes directly
export function middleware() {
  // Middleware disabled - authentication handled in individual API routes
  return
}

export const config = {
  matcher: [],
}
